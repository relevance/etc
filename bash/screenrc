# Copy this file to .screenrc in your home directory
# making screen a little more user friendly

hardstatus on
hardstatus alwayslastline
hardstatus string "%w%=%m/%d %c" 

escape ``  # Instead of Control-a, make the escape/command character be the backtick
autodetach on # Autodetach session on hangup instead of terminating screen completely
defscrollback 30000 # Use a 30000-line scrollback buffer

# startup_message off